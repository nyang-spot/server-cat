version: "3.7"

services:
    psql:
        container_name: cat_psql
        image: postgres
        restart: unless-stopped
        env_file:
            - ../.env
        volumes:
            - ./data/:/var/lib/postgresql/data
        ports:
            - 5432:5432
